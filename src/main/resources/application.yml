server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/simple-app-test
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    properties:
      hibernate:
        fromat-sql: true
